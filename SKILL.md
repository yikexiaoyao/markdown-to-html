---
name: markdown-to-html
description: Convert Markdown files to HTML with support for GitHub Flavored Markdown, tables, code blocks, and syntax highlighting.
version: 1.0.0
---

# Markdown to HTML Converter

Convert Markdown files to HTML format.

## Usage

```bash
# Convert a markdown file to HTML
md2html input.md -o output.html

# Convert with custom CSS
md2html input.md -o output.html --css style.css

# Convert and open in browser
md2html input.md --open
```

## Features

- GitHub Flavored Markdown support
- Table rendering
- Code syntax highlighting
- Custom CSS support
- Auto-generated table of contents